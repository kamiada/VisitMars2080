import React, { FC } from "react";
import { Container } from './langSwitch.styles';
interface Props {
    language: string;
}

export const Switcher: FC<Props> = ({ language }) => {
    const something;
    return (
        <Container>
            <div>

            </div>
        </Container>
    )
}