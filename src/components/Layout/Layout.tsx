import * as React from 'react';
import { Container } from './Layout.styles';
import { FC } from 'react';

interface Props {
    img?: String
}

export const Layout: FC<Props> = ({ img }) => {
    return (
        <Container>

        </Container>
    )
}
